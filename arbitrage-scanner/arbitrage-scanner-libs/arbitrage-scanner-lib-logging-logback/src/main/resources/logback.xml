<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration>
<configuration scan="true" scanPeriod="30 seconds" debug="false">

    <property name="LOGS_FB_HOSTS" value="${LOGS_FB_HOSTS:-localhost}"/>
    <property name="LOGS_FB_PORT" value="${LOGS_FB_PORT:-24224}"/>
    <property name="SERVICE_NAME" value="${SERVICE_NAME:-arbitrage-scanner}"/>

    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{YYYY-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="fluentd" class="ch.qos.logback.more.appenders.DataFluentAppender">
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>INFO</level>
        </filter>

        <tag>app.logs</tag>
        <label>normal</label>
        <remoteHost>${LOGS_FB_HOSTS}</remoteHost>
        <port>${LOGS_FB_PORT}</port>
        <maxQueueSize>20</maxQueueSize>

        <encoder class="net.logstash.logback.encoder.LoggingEventCompositeJsonEncoder">
            <providers>
                <timestamp/>
                <message/>
                <logLevel/>
                <stackTrace/>
                <context/>
                <mdc/>
                <arguments/>
            </providers>
        </encoder>
    </appender>

    <root>
        <appender-ref ref="STDOUT"/>
        <appender-ref ref="fluentd"/>
    </root>
</configuration>